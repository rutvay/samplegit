public class fizbuz{

    // function to check wether rhe number is divisible by 5 or 3 
    public static string check(int number)
    {
        if(number==0)
            return 0;
        return 1;
    }
    
}
